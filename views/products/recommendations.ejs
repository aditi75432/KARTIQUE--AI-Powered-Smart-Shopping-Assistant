<div class="navbar">
  <a href="/products" class="back-btn">&#8592; Go Back to Products</a>
</div>
<div class="recommendation-container">
  <h2 class="rec-title">🧠 Personalized Recommendations</h2>

  <% if (recommendedProducts && recommendedProducts.length > 0) { %>
    <ul class="recommendation-list">
      <% recommendedProducts.forEach(product => { %>
        <li class="rec-item">
          <i class="fa fa-lightbulb"></i>
          <a href="/products/<%= product._id %>"><%= product.title %></a>
        </li>
      <% }) %>
    </ul>
  <% } else { %>
    <p class="no-recs">We’re still learning your preferences. Check back soon! 🕵️‍♀️</p>
  <% } %>
</div>

<style>
  body {
    font-family: 'Segoe UI', sans-serif;
    background-color: #f5f7fa;
    margin: 0;
    padding: 0;
    text-align: center;
  }

  .navbar {
    background: #1a73e8;
    padding: 15px;
    text-align: left;
  }

  .back-btn {
    color: white;
    text-decoration: none;
    font-size: 16px;
    font-weight: bold;
  }

  .recommendation-container {
    padding: 2rem;
    background: #fff8ec;
    border-radius: 12px;
    box-shadow: 0 0 12px rgba(0,0,0,0.1);
  }
  .rec-title {
    font-size: 1.8rem;
    margin-bottom: 1rem;
    color: #333;
  }
  .recommendation-list {
    list-style: none;
    padding-left: 0;
  }
  .rec-item {
    margin: 0.5rem 0;
    font-size: 1.1rem;
  }
  .no-recs {
    font-style: italic;
    color: gray;
  }

</style>
  




